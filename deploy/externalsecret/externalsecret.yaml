# SM-IAM ExternalSecret - 从 Vault 注入敏感配置
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: sm-iam-secret
  namespace: sm-prod
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: sm-iam-secret
    creationPolicy: Owner
  data:
    - secretKey: SM_INTERNAL_API_KEY
      remoteRef:
        key: secret/sm/iam/internal-api-key
        property: value
    - secretKey: SM_JWT_SECRET
      remoteRef:
        key: secret/sm/iam/jwt-secret
        property: value
    - secretKey: SM_SM4_KEY_HEX
      remoteRef:
        key: secret/sm/iam/sm4-key
        property: value
    - secretKey: SM_DATABASE_URL
      remoteRef:
        key: secret/sm/iam/database-url
        property: value
