apiVersion: v1
kind: Service
metadata:
  name: sm-iam
  namespace: sm
  labels:
    app: sm-iam
    platform: sm-suite
spec:
  type: ClusterIP
  selector:
    app: sm-iam
  ports:
  - name: http
    port: 8300
    targetPort: http
    protocol: TCP
